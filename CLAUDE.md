# CLAUDE.md

在处理本仓库前, 先完整读取并遵循 [`AGENTS.md`](AGENTS.md).

## 执行要求

- 将 `AGENTS.md` 作为项目结构, Smali 修改, Android 兼容, 构建验证, 安全和 Git 规则的唯一来源.
- 发现本文件与 `AGENTS.md` 不一致时, 以 `AGENTS.md` 为准.
- 不在本文件重复维护项目规则; 需要调整约束时, 只修改 `AGENTS.md`.
